# in ngrok.yml
authtoken: XXXXXXXXXXXXXXXXXXXXXXXXXX
version: 2
log_level: info
log_format: json
log: /tmp/ngrok.log
region: ap #in
tunnels:
  first:
    addr: 8188
    proto: http
  second:
    addr: 80
    proto: http

